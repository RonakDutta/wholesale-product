/**
 * The product's name, in one place.
 *
 * It was written out by hand in five screens, each with its own copy of the
 * two tone markup, so renaming it meant finding all five. Now they read from
 * here.
 *
 * The wordmark is split so the second half can take the accent colour, which
 * is how it has always been drawn.
 *
 * Note the word "marketplace" still appears throughout the code and the UI as
 * an ordinary noun: the browsing and ordering half of the product, as against
 * the sales book. That is a feature name, not the brand, and it is left alone.
 */
export const BRAND = {
  /** The full name, for a title, a subject line or a sentence. */
  name: "KhazanaBMS",
  /** The wordmark, in two halves. */
  lead: "Khazana",
  tail: "BMS",
};

export default BRAND;
